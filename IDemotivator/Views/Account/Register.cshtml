@model IDemotivator.Models.RegisterViewModel
@{

    ViewBag.Title = Resources.Resource.Register;
}
<link href="~/Content/Register.css" rel="stylesheet">
@using (Html.BeginForm("Register", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
<div class="container">

<div class="row">
    <div class="col-xs-12 col-sm-8 col-md-6 col-sm-offset-2 col-md-offset-3">
        <h2>Please Sign Up <small>It's free and always will be.</small></h2>
        <hr class="colorgraph">
        @Html.AntiForgeryToken()
        <h4>@Resources.Resource.CreateANewAccount.</h4>
        @Html.ValidationSummary("", new { @class = "text-danger" })
        <div class="form-group">
            @Html.TextBoxFor(m => m.Email, new { @class = "form-control", placeholder="Email Address", tabindex="4" })
        </div>
        <div class="row">
            <div class="col-xs-12 col-sm-6 col-md-6">
                <div class="form-group">
                    @Html.PasswordFor(m => m.Password, new { @class = "form-control input-lg", placeholder="Password", tabindex="5" })
                </div>
            </div>
            <div class="col-xs-12 col-sm-6 col-md-6">
                <div class="form-group">
                    @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control input-lg", Placeholder="Confirm Password", tabindex="6" })
                </div>
            </div>
        </div>
        <hr class="colorgraph" align="center">
        <div class="row">
            <div class="col-xs-12 col-md-12"><input type="submit" value=@Resources.Resource.Register class="btn btn-primary btn-block btn-lg" tabindex="7"></div>
        </div>
        </div>
    </div>
</div>
}
@section Scripts {
@Scripts.Render("~/bundles/jqueryval");

}
